# import math library for sin,cos and sqrt
import math
# default value of a,b and beta
# you can set according to your requirement
a = 0
b = 0
beta = 0.0
# Formula of the given equation
X = (3*a+b)/(math.sqrt((a*a)*(math.sin(beta)*(math.sin(beta))))-(b*b)*(math.cos(beta)))
